<% if logged_in? #ログインしているかを判別している%>
  <p>ログイン完了ユーザ: <%= current_user.name %></p>
<% else %>
<div class="center jumbotron">
  <div class="text-center">
    <h1>Welcome!</h1>
    <h4>お気に入りの写真を投稿しよう！</h4>
  </div>
</div>
<% end %>